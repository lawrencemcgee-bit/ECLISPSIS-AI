# Placeholder configuration for ECLISPSIS-AI.
# Real settings will be added in Milestone 2–3.

assistant:
  memory_retention_days: 30
  reduced_motion: true
  theme: "default"

engine:
  mode: "local"

