# Core Python runtime
python-dotenv
requests
aiohttp
pydantic
typing-extensions

# Cross-platform API (Phase 11)
fastapi
uvicorn

# Flet UI (proposed migration from PySide6/QML — see docs/flet_migration_assessment.md)
flet
# flet-cli provides `flet build`/`flet doctor` for packaging — see
# docs/flet_packaging.md. Not needed to just run the app.
flet-cli

# Qt / UI
PySide6==6.10.2

# Audio + Waveform
sounddevice
numpy
scipy

# Real voice I/O — STT (Vosk, offline/free) and TTS (pyttsx3, offline/free,
# uses OS-installed voices). Both optional at runtime; graceful fallback
# to simulated behavior if not installed. See docs/voice_io_assessment.md
# — Vosk also needs a downloaded model, not just this package.
vosk
pyttsx3

# Real vision capture (Tier 3 — see src/services/vision_service.py).
# Optional at runtime; graceful fallback to simulated behavior if not
# installed or no camera is available. Pulls in numpy as its own
# dependency.
opencv-python-headless

# System diagnostics (Phase 9) — optional at runtime; ObservabilityService
# degrades gracefully to {"available": False} if this isn't installed.
psutil

# Agents / Services
beautifulsoup4
lxml
feedparser

# Persistence / Storage
tinydb
jsonschema

# Logging
loguru

# Plugin System
importlib-metadata
importlib-resources

# Tools / Utilities
python-dateutil
tzdata

# Optional Enhancements (safe to include)
markdown
pyyaml
